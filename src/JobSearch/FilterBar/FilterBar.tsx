import React, { FC } from 'react'
import styles from './styles.module.css'

interface FilterBarProps {
 
}

const FilterBar: FC<any> = ({}) => {
    return (
        <div className={styles.filter_bar}>
         
        </div>
    )
}

export default FilterBar